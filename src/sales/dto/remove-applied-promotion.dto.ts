/**
 * Work Unit 6 — DELETE /sales/drafts/:id/promotions/:promotionId
 *
 * Removes an AUTO-applied promotion from the current draft by adding
 * its id to the per-draft veto set. Body intentionally empty.
 */
export class RemoveAppliedPromotionDto {}
